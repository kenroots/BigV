# Wildlife Spotter — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# AI / Computer Vision
opencv-python-headless>=4.9.0
numpy>=1.26.0
ultralytics>=8.1.0        # YOLOv8 — local fallback model
roboflow>=1.1.0           # Roboflow SDK (model download utility)
inference-sdk>=0.9.0      # Roboflow serverless inference API (primary detector)

# HTTP client (for Slack alerts)
aiohttp>=3.9.0

# Utilities
pydantic>=2.6.0
python-dotenv>=1.0.0